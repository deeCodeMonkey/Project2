<!--If there are projects, then loop through them-->
{{#if clients}}
{{#each clients}}
<div class="column">

    <div class="thumbnail" style="background-color: #DBDBDB">
        <a href="/admin/{{client_id}}">
            <img src="/images/logos/{{logo}}">
        </a>
        <div class="caption">
            <h6 class="text-center" style="color: darkslategrey; font-family: 'Raleway', sans-serif;">{{company_name}}</h6>
            <h6 class="text-center" style="color: darkslategrey; font-family: 'Raleway', sans-serif; font-size: small">Client No. {{client_id}}</h6>
                <div class="text-center">
                    <a href="/company/delete/{{client_id}}" data-id="{{client_id}}" class="fi-x delete-icon"></a>
                    <a href="/company/edit/{{client_id}}" data-id="{{client_id}}" class="fi-page-edit edit-icon"></a>
                </div>
        </div>
    </div>

</div>


{{/each}}
{{else}}
<p>No Clients.</p>
{{/if}}


